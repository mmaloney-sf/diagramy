[package]
name = "diagramy"
version = "0.1.0"
edition = "2024"

[build-dependencies]
lalrpop = "0.20"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
lalrpop-util = { version = "0.20", features = ["lexer"] }
regex = "1"
svg = "0.18"
